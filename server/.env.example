PORT=5001
JWT_SECRET=plancraft_super_secret_jwt_key_2025_xperience
# Optional: If not provided, the server automatically boots an in-memory database store
MONGODB_URI=mongodb://localhost:27017/plancraft
# Optional: If not provided, the server automatically uses the deterministic NLP semantic extractor
GEMINI_API_KEY=
